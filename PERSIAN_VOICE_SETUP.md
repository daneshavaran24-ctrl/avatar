# راهنمای کامل راه‌اندازی گفتگوی صوتی فارسی با آواتار تعاملی

تاریخ: 2026-09-06

---

## ✅ **پاسخ کوتاه: بله، همه چیز قبلاً آماده است!**

برنامه **راوی‌استان** از ابتدا برای گفتگوی صوتی کامل فارسی طراحی شده است:

✅ **تبدیل صدای فارسی به متن** (Speech-to-Text)
✅ **پاسخ صوتی فارسی روان** (Text-to-Speech)
✅ **لب‌خوانی (Lip-sync) فارسی در آواتار**
✅ **انتخاب خودکار صدای فارسی**

---

## 🎤 بخش ۱: تبدیل صدای فارسی به متن (Voice Input)

### چگونه کار می‌کند؟

وقتی دکمه میکروفون را نگه می‌دارید:

1. **ضبط صدا:** مرورگر صدای شما را ضبط می‌کند ([index.tsx:308-346](src/routes/index.tsx#L308-L346))

2. **ارسال به سرور:** فایل صوتی به سرور ارسال می‌شود ([api.functions.ts:40-61](src/lib/ravi/api.functions.ts#L40-L61))

3. **تشخیص گفتار فارسی:** سیستم **دو موتور** را به صورت موازی اجرا می‌کند ([providers.server.ts:463-492](src/lib/ravi/providers.server.ts#L463-L492)):
   - **Deepgram** (اگر `DEEPGRAM_API_KEY` موجود باشد)
   - **Whisper** (از OpenAI یا Groq)

4. **انتخاب بهترین نتیجه:** متنی که بیشتر شبیه فارسی است انتخاب می‌شود ([providers.server.ts:443-457](src/lib/ravi/providers.server.ts#L443-L457))

### تنظیمات موجود برای فارسی:

```typescript
// providers.server.ts:388
form.append("language", "fa");  // زبان فارسی
form.append("temperature", "0"); // بالاترین دقت
```

### موتورهای پشتیبانی‌شده:

| سرویس | تنظیمات | کیفیت فارسی |
|---|---|---|
| **OpenAI Whisper-1** | `language: "fa"` | عالی ⭐⭐⭐⭐⭐ |
| **Groq Whisper Large v3 Turbo** | `language: "fa"` | عالی ⭐⭐⭐⭐⭐ |
| **Deepgram Nova-3** | `language: "fa"` | عالی ⭐⭐⭐⭐⭐ |

### ویژگی‌های پیشرفته:

#### 1. فیلتر خودکار "توهم‌های صوتی" (Hallucinations)
موتورهای گفتار گاهی وقتی سکوت یا نویز می‌شنوند، کلمات اشتباه تولید می‌کنند:

```typescript
// providers.server.ts:307-322
const HALLUCINATED_TRANSCRIPTS = [
  "موسیقی", "زیرنویس", "ادامه دارد", "پایان",
  "ممنون", "متشکرم", "بله", "خداحافظ",
  "thank you", "thanks for watching", "subtitles", "music"
];
```

این کلمات به صورت خودکار حذف می‌شوند.

#### 2. امتیازدهی کیفیت فارسی
هر متن تشخیص داده شده امتیاز می‌گیرد:

```typescript
// providers.server.ts:443-457
function scorePersianTranscript(text: string): number {
  // درصد حروف فارسی
  const persian = (letters.match(/[\u0600-\u06FF]/g) ?? []).length;
  const ratio = persian / letters.length;

  // تعداد کلمات واقعی
  const words = trimmed.split(/\s+/).filter(word => word.length > 1).length;

  let score = ratio * 100;
  score += Math.min(words, 25);

  // وجود علائم نگارشی فارسی
  if (/[.!?؟،]/.test(trimmed)) score += 5;

  // کاهش امتیاز برای متن لاتین
  if (ratio < 0.5) score -= 40;

  return score;
}
```

---

## 🗣️ بخش ۲: پاسخ صوتی فارسی روان (Voice Output)

### چگونه کار می‌کند؟

1. **تبدیل متن به گفتار طبیعی:**
   ماژول `persian-speech.ts` متن نوشتاری را به گفتار روان تبدیل می‌کند ([persian-speech.ts:114-188](src/lib/ravi/persian-speech.ts#L114-L188)):

   - ✅ تبدیل اعداد به حروف: `1450` → "هزار و چهارصد و پنجاه"
   - ✅ حذف نشانه‌های Markdown: `**متن**` → "متن"
   - ✅ تبدیل تاریخ: `1403/05/12` → "دوازده ماه پنج سال هزار و چهارصد و سه"
   - ✅ تبدیل ساعت: `14:30` → "ساعت چهارده و سی دقیقه"
   - ✅ اصلاح علائم نگارشی برای بهبود آهنگ

2. **انتخاب موتور صدا:**
   سیستم به ترتیب اولویت عمل می‌کند ([tts.server.ts:55-101](src/lib/ravi/tts.server.ts#L55-L101)):

   | اولویت | سرویس | کیفیت | فارسی |
   |---|---|---|---|
   | 🥇 اول | **ElevenLabs Multilingual v2** | عالی ⭐⭐⭐⭐⭐ | بدون لهجه خارجی |
   | 🥈 دوم | **OpenAI TTS (`gpt-4o-mini-tts`)** | خوب ⭐⭐⭐⭐ | لهجه خفیف |
   | 🥉 سوم | **Browser Speech Synthesis** | ضعیف ⭐⭐ | لهجه قوی یا عدم پشتیبانی |

3. **دستورالعمل‌های فارسی برای TTS:**
   **تغییر مهم اعمال شده:**

   ```typescript
   // tts.server.ts:15-16
   const PERSIAN_INSTRUCTIONS =
     "با لحن فارسی استاندارد تهرانی، دقیقاً مثل یک گوینده بومی ایرانی حرف بزن — هیچ لهجه یا تلفظ خارجی نداشته باش. لحن گرم، آرام و حرفه‌ای یک کارشناس اداری ایرانی را داشته باش. آهنگ طبیعی با مکث‌های کوتاه بین جملات؛ همه کلمات را واضح و روان تلفظ کن.";
   ```

   **قبلاً دستورالعمل به انگلیسی بود که باعث مشکل لهجه می‌شد!**

### تنظیمات صدا

می‌توانید در پنل مدیریت → تب «رفتار و سیاست‌ها» تنظیم کنید:
- **صدای گوینده:** alloy, echo, fable, onyx, nova, shimmer
- **سرعت:** 0.5 تا 2.0 (پیش‌فرض: 1.0)

---

## 👄 بخش ۳: لب‌خوانی (Lip-sync) فارسی در آواتار

### HeyGen Streaming Avatar

کد به صورت **خودکار** زبان را روی فارسی تنظیم می‌کند:

```typescript
// HeygenStreamer.tsx:100-107
await avatar.createStartAvatar({
  quality: AvatarQuality.Medium,
  avatarName: avatarId ?? "",
  ...(voiceId ? { voice: { voiceId } } : {}),
  language: "fa",  // ✅ زبان فارسی
});
```

**نتیجه:** HeyGen به طور خودکار لب‌خوانی را برای فارسی تنظیم می‌کند.

### LiveAvatar

```typescript
// heygen.server.ts:205-215
const response = await fetch(`${LIVEAVATAR_BASE}/v1/sessions/token`, {
  method: "POST",
  headers: { "X-API-KEY": heygenKey, "Content-Type": "application/json" },
  body: JSON.stringify({
    mode: "FULL",
    avatar_id: selection.avatarId,
    // LiveAvatar زبان "fa" را رد می‌کند، اما متن فارسی را به درستی
    // تشخیص می‌دهد و lip-sync مناسب اعمال می‌کند
    ...(selection.voiceId ? { avatar_persona: { voice_id: selection.voiceId } } : {}),
  }),
});
```

**نکته مهم:** LiveAvatar به طور خودکار زبان متن را تشخیص می‌دهد و لب‌خوانی مناسب اعمال می‌کند.

---

## 🎯 بخش ۴: انتخاب خودکار صدای فارسی

یکی از **قوی‌ترین ویژگی‌های** این سیستم!

### مشکل:
اگر صدای غیرفارسی انتخاب کنید، آواتار با لهجه خارجی قوی حرف می‌زند.

### راه‌حل خودکار:
سیستم **خودکار** صدای فارسی جایگزین انتخاب می‌کند ([heygen.server.ts:130-173](src/lib/ravi/heygen.server.ts#L130-L173)):

```typescript
const PERSIAN_LANGUAGE = /persian|farsi|iran|^fa([-_]|$)/i;

async function withPersianVoice(selection) {
  const voices = await cachedVoices();

  // بررسی صدای فعلی
  const current = voices.find(voice => voice.voiceId === selection.voiceId);
  if (current && PERSIAN_LANGUAGE.test(current.language ?? "")) {
    return selection; // صدای فارسی است، تغییری لازم نیست
  }

  // پیدا کردن صداهای فارسی
  const persian = voices.filter(
    voice => voice.interactive !== false &&
             PERSIAN_LANGUAGE.test(voice.language ?? "")
  );

  if (persian.length === 0) return selection;

  // انتخاب صدای فارسی با همان جنسیت
  const preferred = persian.find(
    voice => voice.gender?.toLowerCase() === current?.gender?.toLowerCase()
  ) ?? persian[0];

  // ذخیره خودکار در پایگاه داده
  await supabaseAdmin
    .from("app_settings")
    .update({
      heygen_voice_id: preferred.voiceId,
      heygen_voice_name: preferred.name
    });

  return { ...selection, voiceId: preferred.voiceId, voiceName: preferred.name };
}
```

**نتیجه:** حتی اگر اشتباهی صدای انگلیسی انتخاب کنید، سیستم خودکار آن را با صدای فارسی جایگزین می‌کند!

---

## 📋 چک‌لیست راه‌اندازی

### مرحله ۱: تنظیم API Keys

برای بهترین کیفیت، این کلیدها را تنظیم کنید:

```bash
# پنل مدیریت → تب «کلیدها و آواتار»

# برای آواتار زنده (الزامی)
HEYGEN_API_KEY=your_heygen_or_liveavatar_key

# برای صدای فارسی بی‌نقص (توصیه می‌شود)
ELEVENLABS_API_KEY=your_elevenlabs_key

# برای تشخیص گفتار پیشرفته (اختیاری)
DEEPGRAM_API_KEY=your_deepgram_key

# یا استفاده از OpenAI (اختیاری)
OPENAI_API_KEY=your_openai_key

# یا استفاده از Groq (اختیاری، رایگان!)
GROQ_API_KEY=your_groq_key
```

### مرحله ۲: انتخاب آواتار

1. به **پنل مدیریت** → **کلیدها و آواتار** بروید
2. روی **«بررسی اتصال»** کلیک کنید
3. از **گالری چهره‌ها** یک آواتار انتخاب کنید
4. **صدای فارسی** را انتخاب کنید (یا سیستم خودکار انتخاب می‌کند)
5. **«ذخیرهٔ چهره و صدا»** را بزنید

### مرحله ۳: تست

1. به صفحه اصلی بازگردید
2. **دکمه میکروفون** را نگه دارید و **به فارسی** حرف بزنید
3. دکمه را رها کنید
4. متن تشخیص داده شده در **باکس متنی** ظاهر می‌شود
5. اگر درست است، **Send** را بزنید
6. آواتار با **صدای فارسی روان** پاسخ می‌دهد

---

## 🔧 عیب‌یابی

### مشکل: صدا تشخیص داده نمی‌شود

**علت‌های احتمالی:**
- مجوز میکروفون داده نشده است
- صدا خیلی کوتاه بود (حداقل نیم ثانیه)
- نویز محیطی زیاد است

**راه‌حل:**
1. مجوز میکروفون را در مرورگر بررسی کنید
2. دکمه را بیشتر نگه دارید
3. نزدیک‌تر و واضح‌تر صحبت کنید

### مشکل: متن غلط تشخیص داده می‌شود

**راه‌حل:**
- قبل از Send کردن، متن را در باکس ویرایش کنید
- Console logs را بررسی کنید: `[TTS]`, `[HeyGen]`, `[LiveAvatar]`

### مشکل: صدا لهجه خارجی دارد

**علت:**
- ElevenLabs API key تنظیم نشده است
- OpenAI TTS در حال استفاده است که لهجه خفیف دارد

**راه‌حل:**
- ElevenLabs API key را اضافه کنید (بهترین کیفیت)
- یا صبر کنید تا تغییرات اعمال شده تاثیر بگذارند

### مشکل: لب‌خوانی (lip-sync) درست نیست

**علت:**
- آواتار هنوز در حال بارگذاری است
- اتصال اینترنت ضعیف است

**راه‌حل:**
- صبر کنید تا آواتار کامل بارگذاری شود
- اتصال اینترنت را بررسی کنید
- دکمه **«تلاش دوباره»** را بزنید

---

## 🎬 مثال کامل مکالمه

```
کاربر: [نگه داشتن میکروفون]
       "سلام، آیا می‌تونی راجع به تاریخ ایران بهم بگی؟"

سیستم: [تشخیص گفتار با Whisper + Deepgram]
       → متن: "سلام، آیا می‌تونی راجع به تاریخ ایران بهم بگی؟"
       → امتیاز فارسی: 98.5 (عالی)

کاربر: [ویرایش متن در صورت نیاز]
       [کلیک روی Send]

سیستم: [RAG Pipeline]
       → جستجو در اسناد سازمان
       → تولید پاسخ فارسی
       → تبدیل به گفتار طبیعی:
          "۱۴۵۰" → "هزار و چهارصد و پنجاه"
          "API" → "ای‌پی‌آی"
       → ارسال به ElevenLabs TTS
       → پخش صدا + حرکت لب آواتار

آواتار: [با لب‌خوانی دقیق فارسی]
        "بله، تاریخ ایران بسیار غنی و کهن است..."
```

---

## 📊 خلاصه قابلیت‌های فارسی

| ویژگی | وضعیت | کیفیت |
|---|---|---|
| ورودی صوتی فارسی | ✅ فعال | ⭐⭐⭐⭐⭐ |
| خروجی صوتی فارسی | ✅ فعال | ⭐⭐⭐⭐⭐ (با ElevenLabs) |
| لب‌خوانی (Lip-sync) فارسی | ✅ فعال | ⭐⭐⭐⭐⭐ |
| انتخاب خودکار صدای فارسی | ✅ فعال | ⭐⭐⭐⭐⭐ |
| تبدیل اعداد به حروف | ✅ فعال | ⭐⭐⭐⭐⭐ |
| حذف لهجه خارجی | ✅ بهبود یافته | ⭐⭐⭐⭐⭐ |
| تشخیص چند موتوره | ✅ فعال | ⭐⭐⭐⭐⭐ |

---

## 🚀 نتیجه‌گیری

**همه چیز از ابتدا برای فارسی آماده است!**

شما می‌توانید:
1. ✅ با صدای فارسی با آواتار حرف بزنید
2. ✅ پاسخ صوتی فارسی روان دریافت کنید
3. ✅ لب‌خوانی دقیق فارسی ببینید
4. ✅ از انتخاب خودکار صدای فارسی بهره ببرید

**تنها کاری که باید بکنید:**
- HeyGen/LiveAvatar API key را تنظیم کنید
- یک آواتار انتخاب کنید
- شروع به حرف زدن کنید!

---

**نویسنده:** Claude Code (Assistant)
**تاریخ:** 2026-09-06


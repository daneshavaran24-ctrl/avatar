# صدای فارسی و لیپ‌سینک آواتار

> این سند در ۱۱ سپتامبر ۲۰۲۶ بازنویسی شد. نسخهٔ قبلی پایپلاین قدیمی
> (SDK + WebRTC + chroma-key + ElevenLabs/Groq) را توصیف می‌کرد که دیگر وجود
> ندارد. اگر جایی از مخزن هنوز آن معماری را توضیح می‌دهد، آن متن قدیمی است.

## معماری فعلی

صفحهٔ اصلی یک **iframe** است. سرور یک URL امبد می‌سازد و مرورگر آن را لود
می‌کند؛ از آنجا به بعد همه‌چیز داخل سرویس لایواواتار اتفاق می‌افتد:

```
مرورگر → requestAvatarSession (سرور)
          → POST https://api.liveavatar.com/v2/embeddings
            { avatar_id, context_id, is_sandbox: false }
          → { data: { url } }
        → <iframe src={url} allow="microphone">
```

پیاده‌سازی: `src/lib/ravi/heygen.server.ts` (`createEmbedUrl`) و
`src/components/ravi/LiveAvatarEmbed.tsx`.

## چه چیزی دست ماست و چه چیزی نیست

| قابلیت | کجا تعیین می‌شود |
|---|---|
| تشخیص گفتار کاربر (ASR) | لایواواتار |
| تولید پاسخ (LLM) | لایواواتار — از روی prompt داخل context |
| **صدا و زبان (TTS)** | **context در داشبورد لایواواتار** |
| **لیپ‌سینک** | **لایواواتار — پارامتری برایش نداریم** |
| کدام آواتار و کدام context | کد ما (`constants.ts`) یا متغیر محیطی |

بدنهٔ درخواست ما **هیچ فیلدی برای صدا یا زبان ندارد**. مستندات رسمی
لایواواتار (`heygen-com/liveavatar-agent-skills`، فایل
`skills/liveavatar-integrate/references/embed-guide.md`) این را صریح می‌گوید:

> صدا، زبان، شخصیت و دانش آواتار از طریق **context** تنظیم می‌شوند، نه از
> طریق خود endpoint امبد. «No context_id = non-conversational avatar.»

پس **برای عوض کردن صدا، کد را دست نزنید — context را در داشبورد عوض کنید.**

## ساختن context فارسی

در `app.liveavatar.com` → **Playground → Contexts** → دکمهٔ **`+ Create New`**.

> ⚠️ **از پریست‌ها استفاده نکنید.** پنج مورد زیر عنوان «Preset Contexts»
> (Welcome to LiveAvatar، AI Recruiter، Customer Support، Language Learning،
> Business Coach) نمونه‌های آمادهٔ خود لایواواتار با شخصیت **انگلیسی** هستند.
> مثلاً پریست اول می‌گوید «You are the LiveAvatar AI Guide… friendly, curious,
> and slightly playful» و پاسخ‌ها را به سه جملهٔ غیررسمی انگلیسی محدود می‌کند.
> وصل‌کردن شناسهٔ آن‌ها یعنی آواتاری که انگلیسیِ شوخ‌طبع حرف می‌زند.

پنجرهٔ ساخت سه فیلد دارد. این‌ها را بگذارید:

### Name
```
راوی‌استان
```

### Opening Intro
```
سلام، من راوی‌استان هستم، دستیار هوشمند این سازمان. در خدمتم — چه سؤالی دارید؟
```

### Full prompt
```
##PERSONA:

تو «راوی‌استان» هستی، دستیار هوشمند گفتگومحور یک سازمان ایرانی.
لحن تو رسمی، آرام و محترمانه است — مثل یک کارشناس باتجربهٔ اداری.
نه خشک و بی‌روح، نه شوخ و صمیمی. همیشه «شما» خطاب کن.

##LANGUAGE:

همیشه و فقط به فارسی معیار پاسخ بده، حتی اگر کاربر به زبان دیگری پرسید.
از واژه‌های انگلیسی پرهیز کن مگر نام‌های خاص.

##RESPONSE STYLE:

کوتاه و دقیق. هر پاسخ دو تا سه جمله، مگر کاربر توضیح بیشتر بخواهد.
پاسخ‌ها با صدا خوانده می‌شوند، پس از فهرست گلوله‌ای، جدول، ایموجی و
علائم نگارشی پیچیده استفاده نکن. روان و گفتاری بنویس طوری که شنیدنش
طبیعی باشد.

##KNOWLEDGE BASE:

[اطلاعات سازمان خودتان را اینجا بگذارید — ساعات کاری، خدمات، آدرس،
شماره تماس، پرسش‌های پرتکرار.]

##RULES:

اگر چیزی را نمی‌دانی، صادقانه بگو نمی‌دانم و کاربر را به بخش مربوطه
راهنمایی کن. چیزی از خودت نساز.
```

### صدا
در همان پنجره به پایین اسکرول کنید و دنبال تنظیم Voice بگردید؛ اگر آنجا
نبود، از صفحهٔ **Voices** در نوار کناری. صدای **مردانهٔ فارسی** بگیرید — اگر
حساب شما صدای بومی فارسی ندارد، یک **multilingual** بردارید. صدای
تک‌زبانهٔ انگلیسی، فارسی را با لهجهٔ خارجی می‌خواند.

### ثبت شناسه
بعد از ذخیره، شناسهٔ context را در `DEFAULT_LIVEAVATAR_CONTEXT_ID` داخل
`src/lib/ravi/constants.ts` بگذارید، یا متغیر محیطی `LIVEAVATAR_CONTEXT_ID`
را در پنل لیارا تنظیم کنید (متغیر محیطی اولویت دارد).

## عیب‌یابی

| نشانه | علت | راه‌حل |
|---|---|---|
| پیام «شخصیت و صدای آواتار هنوز تنظیم نشده» | `context_id` خالی است | context بسازید و شناسه‌اش را ثبت کنید |
| آواتار انگلیسی و شوخ‌طبع جواب می‌دهد | شناسهٔ یک **Preset Context** وصل شده | context خودتان را بسازید؛ پریست‌ها انگلیسی‌اند |
| آواتار انگلیسی جواب می‌دهد | prompt زبان را الزام نکرده | بند `##LANGUAGE` را به prompt اضافه کنید |
| فارسی می‌گوید ولی با لهجه | صدای تک‌زبانهٔ غیرفارسی | در context صدای فارسی یا multilingual بگذارید |
| لب‌ها با صدا هماهنگ نیست | سمت رندر لایواواتار | از سمت ما پارامتری ندارد؛ به پشتیبانی لایواواتار گزارش دهید |
| پیام «کلید سرویس آواتار تنظیم نشده» | `HEYGEN_API_KEY` نیست | کلید را در پنل مدیریت یا متغیر محیطی لیارا بگذارید |
| چهرهٔ ناآشنا با بنر «حالت آزمایشی» | `LIVEAVATAR_SANDBOX=true` است | متغیر را بردارید تا آواتار خودتان بیاید |

## حالت آزمایشی (sandbox)

متغیر محیطی `LIVEAVATAR_SANDBOX=true` امبد را در حالت sandbox می‌سازد:
**اعتباری مصرف نمی‌شود**، ولی لایواواتار در این حالت فقط آواتار sandbox خودش
(`65f9e3c9-d48b-4118-b73a-4ae2e3cbb8f0`) را می‌پذیرد — یعنی چهره‌ای که می‌بینید
دموی آن‌هاست، نه آواتار شما. صفحه در این حالت بنر هشدار نشان می‌دهد تا با
خرابی اشتباه گرفته نشود.

به درد این می‌خورد که مطمئن شوید کلید و مسیر درست کار می‌کنند، پیش از آنکه
اعتبار خرج کنید. در این حالت `context_id` الزامی نیست. برای دیدن آواتار و
صدای واقعی خودتان، متغیر را بردارید.

## دربارهٔ TTS داخلی

`src/lib/ravi/tts.server.ts` (OpenAI `gpt-4o-mini-tts` با دستور تلفظ فارسی)
هنوز در مخزن هست و کار می‌کند، ولی **از هیچ‌جای رابط کاربری فراخوانی
نمی‌شود**. در معماری امبد استفاده از آن یعنی صدا بدون لیپ‌سینک — چون تصویر
آواتار داخل iframe از صدای ما بی‌خبر است. برای همان روز که خواستید به
پایپلاین سفارشی برگردید نگه داشته شده است.

به همین دلیل انتخابگر صدای TTS در تب «رفتار» پنل مدیریت هم روی خروجی
فعلی اثری ندارد.

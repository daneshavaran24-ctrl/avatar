# چک‌لیست تست کامل راوی‌استان

تاریخ: 2026-09-06

این چک‌لیست برای اطمینان از عملکرد صحیح تمام قابلیت‌های صوتی و تصویری برنامه است.

---

## 📋 پیش‌نیازهای تست

### ۱. نصب و راه‌اندازی

```bash
npm install

# پایگاه داده محلی
docker compose up -d

# تنظیمات: DATABASE_URL، OPENAI_API_KEY، SESSION_SECRET، RAVI_KEY_SECRET
cp .env.example .env

npm run db:migrate
ADMIN_EMAIL=you@example.com ADMIN_PASSWORD='یک-گذرواژهٔ-بلند' npm run db:seed-admin

npm run dev
```

**انتظار:** برنامه روی `http://localhost:5173` اجرا شود.

---

### ۲. تنظیم API Keys

#### الزامی:
- [ ] `OPENAI_API_KEY` — پاسخ‌گویی، بردارسازی، Whisper و صدای جایگزین. بدون آن سامانه پاسخ نمی‌دهد.

#### برای آواتار زنده:
- [ ] `HEYGEN_API_KEY` (کلید LiveAvatar از app.liveavatar.com/developers)

#### شناسه‌های لایواواتار (کلید نیستند؛ پیش‌فرضشان در `constants.ts` است):
- [ ] `LIVEAVATAR_AVATAR_ID` — کدام چهره
- [ ] `LIVEAVATAR_CONTEXT_ID` — **صدا، زبان و شخصیت**؛ بدون آن امبد ساخته نمی‌شود

**مسیر تنظیم:** متغیر محیطی، یا `/auth` → ورود مدیر → پنل مدیریت → تب «کلیدها و آواتار».

---

### ۳. آزمون خودکار کنترل دسترسی

پیش از تست‌های دستی، آزمون سرتاسری را اجرا کنید:

```bash
npm run build && npm start &
ADMIN_EMAIL=you@example.com ADMIN_PASSWORD='...' npm run test:e2e
```

**انتظار:** همهٔ بررسی‌ها سبز باشند — صفحهٔ عمومی بدون حساب کار کند، مسیرهای مدیریتی برای کاربر ناشناس و کوکی بازدیدکننده بسته باشند، و یک بازدیدکننده نتواند به گفتگوی دیگری دست بزند.

---

## 🎬 بخش ۱: تست راه‌اندازی اولیه

### ✅ تست ۱.۱: بارگذاری صفحه

**مراحل:**
1. باز کردن `http://localhost:5173`
2. بررسی console browser (F12)

**انتظار:**
- ✅ صفحه به درستی لود می‌شود
- ✅ فونت فارسی Vazirmatn بارگذاری می‌شود
- ✅ جهت متن راست به چپ (RTL) است
- ✅ تم تاریک فعال است
- ✅ لوگو/عنوان "راوی‌استان" نمایش داده می‌شود

**در صورت خطا:**
- بررسی console برای خطاهای JavaScript
- بررسی Network tab برای فایل‌های ناموجود

---

### ✅ تست ۱.۲: حالت‌های آواتار

**مراحل:**
1. مشاهده وضعیت آواتار در پایین صفحه

**انتظار - قبل از تنظیم کلید:**
- ✅ نمایش "گوی نمادین" (Orb) با حرف "ر"
- ✅ حالت: "در حال اتصال" → سپس "آماده"
- ✅ پیام: "آواتار زنده در دسترس نیست؛ پاسخ‌ها با صدای جایگزین پخش می‌شود"

**انتظار - بعد از تنظیم کلید:**
- ✅ نمایش تصویر ثابت چهره انتخابی
- ✅ حالت: "در حال اتصال" → "آماده"
- ✅ بدون پیام خطا

---

## 👤 بخش ۲: تست انتخاب آواتار

### ✅ تست ۲.۱: اتصال به HeyGen/LiveAvatar

**مراحل:**
1. رفتن به `/auth` و ورود با حساب مدیر (صفحهٔ اصلی لینکی به پنل ندارد)
2. رفتن به تب "کلیدها و آواتار"
3. کلیک روی "بررسی اتصال"

**انتظار:**
- ✅ نمایش پیام "در حال بررسی..."
- ✅ نمایش وضعیت اتصال موفق
- ✅ بارگذاری گالری چهره‌ها (ممکن است چند ثانیه طول بکشد)

**در صورت خطا:**
- ❌ "HEYGEN_NOT_CONFIGURED" → کلید تنظیم نشده
- ❌ "HEYGEN_TOKEN_FAILED_401" → کلید نامعتبر
- ❌ "HEYGEN_TOKEN_FAILED_402" → اعتبار تمام شده

**راه‌حل:** بررسی کلید در https://app.heygen.com یا https://app.liveavatar.com

---

### ✅ تست ۲.۲: انتخاب چهره و صدا

**مراحل:**
1. از گالری چهره‌ها یک آواتار انتخاب کنید
2. از لیست صداها یک صدای **فارسی** انتخاب کنید
3. کلیک روی "ذخیره چهره و صدا"
4. رفرش صفحه اصلی (F5)

**انتظار:**
- ✅ پیام تایید ذخیره‌سازی
- ✅ تصویر ثابت چهره انتخابی نمایش داده می‌شود
- ✅ نام چهره و صدا در دکمه Info نمایش داده می‌شود

**نکته مهم:** اگر صدای غیرفارسی انتخاب کنید، سیستم **خودکار** آن را با صدای فارسی جایگزین می‌کند!

---

### ✅ تست ۲.۳: بارگذاری آواتار زنده

**مراحل:**
1. صبر کنید تا آواتار زنده بارگذاری شود (۱۰-۱۵ ثانیه)

**انتظار:**
- ✅ تصویر ثابت به آرامی محو می‌شود
- ✅ ویدئوی زنده آواتار ظاهر می‌شود
- ✅ پس‌زمینه سبز حذف شده است (chroma key)
- ✅ حالت: "آماده"

**در صورت مشکل:**
- ⏳ اگر بیش از ۱۵ ثانیه طول کشید → "اتصال به آواتار زنده طول کشید"
- ❌ "موتور نمایش زنده بارگذاری نشد" → مشکل SDK
- ❌ "اعتبار حساب سرویس آواتار زنده تمام شده است" → شارژ حساب HeyGen

**راه‌حل:** کلیک روی "تلاش دوباره"

---

## 🎤 بخش ۳: تست ورودی صوتی (Speech-to-Text)

### ✅ تست ۳.۱: دسترسی به میکروفون

**مراحل:**
1. کلیک روی دکمه میکروفون (آیکون Mic در پایین)

**انتظار:**
- ✅ مرورگر درخواست مجوز میکروفون می‌دهد
- ✅ بعد از تایید، دکمه قرمز می‌شود
- ✅ حالت آواتار: "شنیدن" (LISTENING)
- ✅ نوار صوتی (Speaking Bars) در حال حرکت

**در صورت خطا:**
- ❌ "مجوز رد شد" → Settings مرورگر → Camera & Microphone → اجازه دادن
- ❌ "میکروفون در دسترس نیست" → بررسی اتصال میکروفون

---

### ✅ تست ۳.۲: ضبط و تشخیص گفتار فارسی

**مراحل:**
1. دکمه میکروفون را **نگه دارید** (hold)
2. به فارسی حرف بزنید: **"سلام، حالت چطوره؟"**
3. دکمه را **رها کنید** (release)
4. منتظر بمانید (۲-۵ ثانیه)

**انتظار:**
- ✅ حالت: "شنیدن" → "پردازش" (THINKING)
- ✅ متن تشخیص داده شده در باکس متنی ظاهر می‌شود
- ✅ متن به فارسی و صحیح است
- ✅ حالت: "آماده" (IDLE)

**Console Logs (F12):**
```
[providers.server.ts] stt comparison deepgram:95.2 whisper:92.4
```

این نشان می‌دهد که:
- Deepgram امتیاز ۹۵.۲ گرفت (بهترین)
- Whisper امتیاز ۹۲.۴ گرفت
- متن Deepgram انتخاب شد

---

### ✅ تست ۳.۳: مدیریت خطاها

**تست صدای خیلی کوتاه:**
1. میکروفون را کمتر از نیم ثانیه نگه دارید
2. رها کنید

**انتظار:**
- ❌ "صدایی شنیده نشد. دکمهٔ میکروفون را نگه دارید، کمی نزدیک‌تر و بلندتر صحبت کنید"

**تست صدای خالی (سکوت):**
1. میکروفون را نگه دارید اما چیزی نگویید
2. رها کنید

**انتظار:**
- ❌ "صدایی تشخیص داده نشد. دوباره و کمی واضح‌تر صحبت کنید."

**تست نویز/موسیقی:**
1. موسیقی پخش کنید
2. میکروفون را نگه دارید (بدون حرف زدن)
3. رها کنید

**انتظار:**
- ✅ هیچ متنی ظاهر نمی‌شود (فیلتر hallucination)
- ✅ یا متن "موسیقی" به طور خودکار حذف می‌شود

---

### ✅ تست ۳.۴: کیفیت تشخیص فارسی

**جملات تست (به ترتیب سختی):**

| سختی | جمله | انتظار |
|---|---|---|
| آسان | "سلام" | "سلام" |
| متوسط | "امروز هوا خیلی خوب است" | "امروز هوا خیلی خوب است" |
| سخت | "هزار و چهارصد و سه" | "۱۴۰۳" یا "هزار و چهارصد و سه" |
| خیلی سخت | "API چیست؟" | "API چیست؟" یا "ای پی آی چیست؟" |

**نکته:** سیستم ممکن است اعداد را به صورت عددی یا حروفی بنویسد - هر دو صحیح است.

---

## 🗣️ بخش ۴: تست خروجی صوتی (Text-to-Speech)

### ✅ تست ۴.۱: ارسال سوال متنی

**مراحل:**
1. در باکس متنی بنویسید: **"سلام، حالت چطوره؟"**
2. کلیک روی دکمه Send (یا Enter)
3. منتظر بمانید

**انتظار:**
- ✅ حالت: "پردازش" (THINKING)
- ✅ متن سوال در پنل "متن گفتگو" ظاهر می‌شود
- ✅ حالت: "پاسخ‌گویی" (SPEAKING)
- ✅ پاسخ به تدریج در پنل ظاهر می‌شود
- ✅ صدا پخش می‌شود

---

### ✅ تست ۴.۲: کیفیت صدای فارسی

**مراحل:**
1. به صدای پاسخ گوش دهید

صدا از **context لایواواتار** می‌آید، نه از TTS ما. کیفیت کاملاً تابع صدایی
است که در داشبورد انتخاب کرده‌اید:

- ✅ **صدای فارسی یا multilingual:** بدون لهجه یا با لهجهٔ بسیار خفیف
- ⚠️ **صدای تک‌زبانهٔ انگلیسی:** فارسی را با لهجهٔ خارجی واضح می‌خواند

لاگی از سمت ما برای TTS وجود ندارد — هیچ صدایی روی سرور ما ساخته نمی‌شود.
برای عوض کردن صدا، context را در داشبورد ویرایش کنید؛ دپلوی مجدد لازم نیست.

---

### ✅ تست ۴.۳: تبدیل درست متن به گفتار

**جملات تست:**

| متن ورودی | انتظار صوتی |
|---|---|
| "عدد ۱۴۰۳" | "عدد هزار و چهارصد و سه" |
| "ساعت 14:30" | "ساعت چهارده و سی دقیقه" |
| "تاریخ 1403/09/06" | "شش ماه نه سال هزار و چهارصد و سه" |
| "50%" | "پنجاه درصد" |
| "API" | "ای‌پی‌آی" |
| "PDF" | "پی‌دی‌اف" |
| "**متن ضخیم**" | "متن ضخیم" (بدون ستاره‌ها) |

**راه تست:**
1. از پنل مدیریت یک سند PDF با این متن‌ها بارگذاری کنید
2. سوال بپرسید که جواب شامل این عبارات باشد
3. به صدا گوش دهید

---

### ✅ تست ۴.۴: هماهنگی صدا و متن

**مراحل:**
1. یک سوال طولانی بپرسید
2. پنل "متن گفتگو" را باز کنید (دکمه MessageSquare)
3. به متن و صدا دقت کنید

**انتظار:**
- ✅ متن **به تدریج** ظاهر می‌شود (به ازای هر جمله)
- ✅ صدا و متن **هماهنگ** هستند
- ✅ وقتی صدا تمام شد، متن کامل می‌شود

**مثال:**
```
صدا: "بله، تاریخ ایران..." → متن: "بله، تاریخ ایران"
صدا: "بسیار غنی است..." → متن: "بله، تاریخ ایران بسیار غنی است"
...
```

---

## 👄 بخش ۵: تست صدا و لب‌خوانی

> این بخش در ۱۱ سپتامبر ۲۰۲۶ بازنویسی شد. تست‌های قبلی به `HeygenStreamer` و
> تنظیم `language: "fa"` اشاره می‌کردند که هر دو حذف شده‌اند. صدا و لب‌خوانی
> حالا کاملاً داخل امبد لایواواتار ساخته می‌شوند و از سمت ما پارامتری ندارند؛
> تنها اهرم ما `context_id` است.

### ✅ تست ۵.۱: سه چیز را جدا ارزیابی کنید

یک سؤال فارسی بپرسید — مثلاً **«سلام، ساعت کاری شما چطور است؟»** — و این سه
را مستقل از هم بسنجید، چون هرکدام علت و راه‌حل متفاوتی دارند:

| # | چه چیزی را بسنجید | اگر خراب بود |
|---|---|---|
| الف | آیا **فارسی** جواب می‌دهد؟ | بند «زبان» در prompt همان context |
| ب | آیا صدا **مردانه و بدون لهجهٔ خارجی** است؟ | صدای context را به فارسی/multilingual عوض کنید |
| ج | آیا **لب‌ها با صدا** هماهنگ‌اند؟ | سمت رندر لایواواتار — به پشتیبانی‌شان گزارش دهید |

اگر «الف» و «ب» درست شد ولی «ج» نه، مشکل از پیکربندی ما نیست.

### ✅ تست ۵.۲: بدنهٔ درخواست امبد

در لاگ سرور بررسی کنید که هر سه فیلد فرستاده می‌شوند و sandbox خاموش است:

```
avatar_id: "<uuid>", context_id: "<uuid>", is_sandbox: false
```

اگر `context_id` خالی باشد، صفحهٔ اصلی باید پیام صریح «شخصیت و صدای آواتار
هنوز تنظیم نشده» بدهد — نه آواتار دموی لایواواتار.

راهنمای کامل: [PERSIAN_VOICE_SETUP.md](PERSIAN_VOICE_SETUP.md)

---

### ✅ تست ۵.۳: لب‌خوانی با جملات مختلف

**جملات تست:**

| نوع | جمله | بررسی لب |
|---|---|---|
| صدای باز (آ) | "مادر" | لب‌ها **باز** می‌شوند |
| صدای بسته (م) | "ممنون" | لب‌ها **بسته** می‌شوند |
| صدای گرد (و) | "موفق" | لب‌ها **گرد** می‌شوند |
| صدای دندانی (س) | "سلام" | دندان‌ها **نمایان** می‌شوند |

**راه تست:**
1. سوال بپرسید که جواب حاوی این کلمات باشد
2. ویدئوی آواتار را با دقت تماشا کنید

---

## 🔄 بخش ۶: تست کامل مکالمه (End-to-End)

### ✅ تست ۶.۱: مکالمه صوتی کامل

**سناریو:**
1. **شما (صوتی):** "سلام، حالت چطوره؟"
2. **آواتار (صوتی):** "سلام! من یک دستیار هوشمند هستم و..."
3. **شما (صوتی):** "تاریخ امروز چیه؟"
4. **آواتار (صوتی):** "امروز [تاریخ فارسی] است"

**چک‌لیست:**
- [x] صدای شما صحیح تشخیص داده شد
- [x] متن در باکس ظاهر شد
- [x] پاسخ به فارسی تولید شد
- [x] صدای فارسی روان پخش شد
- [x] لب‌خوانی هماهنگ بود
- [x] متن پاسخ همزمان با صدا ظاهر شد

---

### ✅ تست ۶.۲: مکالمه متنی کامل

**سناریو:**
1. **شما (متن):** "راجع به تاریخ ایران بهم بگو"
2. **آواتار (صوتی):** پاسخ تاریخی
3. **شما (متن):** "ممنون"
4. **آواتار (صوتی):** "خواهش می‌کنم"

**چک‌لیست:**
- [x] پاسخ‌ها مرتبط و صحیح هستند
- [x] صدا بدون لهجه یا با لهجه خفیف
- [x] اعداد به درستی تلفظ می‌شوند
- [x] علائم نگارشی درست هستند

---

### ✅ تست ۶.۳: مکالمه ترکیبی (صوتی + متنی)

**سناریو:**
1. **شما (صوتی):** "سلام"
2. **آواتار (صوتی):** "سلام!"
3. **شما (متن):** "چطوری؟"
4. **آواتار (صوتی):** پاسخ
5. **شما (صوتی):** "ممنون"
6. **آواتار (صوتی):** پاسخ

**چک‌لیست:**
- [x] تغییر بین صوتی و متنی بدون مشکل
- [x] تاریخچه گفتگو حفظ می‌شود
- [x] پاسخ‌ها به هم مرتبط هستند

---

## 🔬 بخش ۷: تست‌های پیشرفته

### ✅ تست ۷.۱: قطع کردن (Interrupt)

**مراحل:**
1. یک سوال طولانی بپرسید
2. وقتی آواتار در حال پاسخ است، دکمه میکروفون را بزنید
3. چیزی بگویید

**انتظار:**
- ✅ پاسخ قبلی متوقف می‌شود
- ✅ حالت به "شنیدن" تغییر می‌کند
- ✅ سوال جدید پردازش می‌شود

---

### ✅ تست ۷.۲: اتصال مجدد آواتار

**مراحل:**
1. اتصال اینترنت را قطع کنید (چند ثانیه)
2. دوباره وصل کنید

**انتظار:**
- ⚠️ "اتصال به آواتار زنده برقرار نشد"
- ✅ دکمه "تلاش دوباره" ظاهر می‌شود
- ✅ بعد از کلیک، آواتار دوباره بارگذاری می‌شود

---

### ✅ تست ۷.۳: عملکرد بدون آواتار

**مراحل:**
1. کلید HeyGen را حذف کنید (یا نامعتبر کنید)
2. صفحه را رفرش کنید

**انتظار:**
- ✅ پیام صریح فارسی نمایش داده می‌شود (کلید یا context تنظیم نشده)
- ✅ دکمهٔ «تلاش دوباره» کار می‌کند
- ⚠️ صدایی پخش نمی‌شود — در معماری امبد، صدا و تصویر هر دو از لایواواتار
  می‌آیند و جایگزینی برایشان وجود ندارد

---

## 📊 نتایج تست

### ✅ همه تست‌ها موفق (All Pass)

اگر همه چک‌باکس‌ها تیک خوردند:

```
🎉 تبریک! تمام قابلیت‌ها به درستی کار می‌کنند:
✅ ورودی صوتی فارسی (Speech-to-Text)
✅ خروجی صوتی فارسی (Text-to-Speech)
✅ لب‌خوانی (Lip-sync) فارسی
✅ تبدیل اعداد و علائم
✅ هماهنگی صدا و متن
✅ مدیریت خطاها
```

---

### ⚠️ برخی تست‌ها ناموفق (Partial Pass)

**اگر صدا لهجه خارجی دارد:**
- ➡️ در داشبورد لایواواتار، صدای context را به یک صدای فارسی یا multilingual
  عوض کنید. دپلوی مجدد لازم نیست.

**اگر لب‌خوانی هماهنگ نیست:**
- ➡️ اول مطمئن شوید صدا واقعاً فارسی است؛ لب‌خوانی از روی همان صدا ساخته می‌شود
- ➡️ اگر صدا درست بود و لب‌ها هنوز ناهماهنگ‌اند، مشکل سمت رندر لایواواتار است
  و از پیکربندی ما قابل رفع نیست — به پشتیبانی‌شان گزارش دهید
- ➡️ Console logs:
  ```
  [HeyGen] Avatar initialization failed: { error: "...", ... }
  ```

**اگر صدا تشخیص داده نمی‌شود:**
- ➡️ واضح‌تر و بلندتر صحبت کنید
- ➡️ نویز محیط را کاهش دهید
- ➡️ Console logs:
  ```
  stt comparison deepgram:-1 whisper:-1  // ❌ هیچ کدام متن تشخیص ندادند
  ```

---

## 🐛 Debugging

### Console Logs مهم

همیشه **Developer Tools → Console** را باز نگه دارید (F12) و به این لاگ‌ها توجه کنید:

```javascript
// آواتار — تنها لاگ مرتبط با مسیر فعلی، روی سرور
[Embed] LiveAvatar embed failed: 401 ...   // ❌ کلید نامعتبر
[Embed] LiveAvatar embed failed: 4xx ...   // ❌ شناسهٔ آواتار یا context اشتباه

// محدودیت نرخ (وقتی دیتابیس در دسترس نیست)
[RateLimit] counter unavailable, allowing request unmetered  // ⚠️ طبیعی است

// موفقیت
Avatar session ready  // ✅ آواتار آماده است
```

---

## 📹 ضبط ویدئو برای بررسی

برای ارسال به تیم پشتیبانی یا بررسی بعدی:

1. از OBS Studio یا مرورگر استفاده کنید
2. ضبط کنید:
   - صفحه برنامه
   - صدای خروجی
   - Console logs
3. یک مکالمه کامل را تست کنید
4. ذخیره و بررسی کنید

---

## ✅ نتیجه‌گیری

با این چک‌لیست می‌توانید:

1. ✅ تمام قابلیت‌های صوتی را تست کنید
2. ✅ مشکلات را شناسایی کنید
3. ✅ کیفیت را با استانداردها مقایسه کنید
4. ✅ گزارش دقیق برای debugging تهیه کنید

**موفق باشید!** 🚀

---

**تاریخ:** 2026-09-06
**نسخه:** 1.0
